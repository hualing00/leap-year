#include <stdio.h>
int leapyear()
{ 
	int i;
	scanf("%d", &i);
	if (i % 400 == 0 || (i % 4 == 0 && i % 100 != 0))
		return 1;
	else
		return 0;
}
int main()
{
	int j=leapyear();
	if (j == 1)
		printf("是闰年");
	else
		printf("不是闰年");
	return 0;
}
