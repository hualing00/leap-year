#include<stdio.h>
int main()
{
	int year;
	for (year = 2000; year <= 2500; year++)
	{
		if (year % 4 == 0 && year % 100 != 0)
		{
			printf("%d ", year);
		}
		else if (year % 400 == 0)
		{
			printf("%d ", year);
		}

	}
	return 0;
}
